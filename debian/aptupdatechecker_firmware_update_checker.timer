[Unit]
Description=Checks for firmware updates (timer)

[Timer]
OnCalendar=6 hours
OnStartupSec=60m
RandomizedDelaySec=60m
Persistent=true

[Install]
WantedBy=timers.target
WantedBy=aptupdatechecker.service
WantedBy=graphical.target
WantedBy=multi-user.target